# Intentionally minimal — all config is in application.yml
server.port=8080
server.address=10.30.1.169

spring.datasource.url=jdbc:postgresql://aws-1-ap-southeast-1.pooler.supabase.com:6543/postgres?sslmode=require
spring.datasource.username=postgres.cmqemciyfhpmbgikzoes
spring.datasource.password=RoadEye_1234#

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Tilt config
roadeye.tilt.threshold=45.0
roadeye.tilt.cooldown-seconds=300

# Twilio SMS
twilio.account-sid=YOUR_SID
twilio.auth-token=YOUR_TOKEN
twilio.phone-number=YOUR_NUMBER